"use strict";document.querySelectorAll(".main-link").forEach((link)=>{link.addEventListener("click",()=>{const target=link.dataset.target;const submenu=document.querySelector(`[data-submenu="${target}"]`);document.querySelectorAll(".submenu").forEach((s)=>{if(s!==submenu)s.classList.add("d-none");});submenu.classList.toggle("d-none");document.querySelectorAll(".main-link").forEach((l)=>l.classList.remove("active"));link.classList.add("active");const firstSubLink=submenu?.querySelector(".sub-link");if(firstSubLink)firstSubLink.click();})});function activateFirstGlobalTabIn(container){const firstBtn=container.querySelector(".global-tab-btn");if(firstBtn){firstBtn.click();return}
const contents=container.querySelectorAll(".global-tab-content");if(contents.length){contents.forEach((c)=>c.classList.remove("active"));contents[0].classList.add("active")}}
document.querySelectorAll(".sub-link").forEach((link)=>{link.addEventListener("click",()=>{const targetId=link.dataset.content;document.querySelectorAll(".content-box").forEach((box)=>{box.classList.remove("active")});const selected=document.getElementById(targetId);if(selected){selected.classList.add("active");activateFirstGlobalTabIn(selected)}
const submenu=link.closest(".submenu");submenu?.querySelectorAll(".sub-link").forEach((l)=>l.classList.remove("active"));link.classList.add("active")})});document.addEventListener("click",(e)=>{const button=e.target.closest(".global-tab-btn");if(!button)return;const tab=button.dataset.tab;const container=button.closest(".content-box")||document;container.querySelectorAll(".global-tab-btn").forEach((btn)=>btn.classList.remove("active"));button.classList.add("active");container.querySelectorAll(".global-tab-content").forEach((content)=>{if(content.dataset.tabContent===tab){content.classList.add("active")}else{content.classList.remove("active")}})});document.querySelectorAll(".dropdown-toggle-box").forEach((toggle)=>{toggle.addEventListener("click",()=>{toggle.parentElement.classList.toggle("open")})});const switchTypeEl=document.getElementById("switchType");if(switchTypeEl){switchTypeEl.addEventListener("change",function(){const isScheduled=this.value==="scheduled";document.querySelector(".scheduled-section")?.classList.toggle("d-none",!isScheduled);document.querySelector(".auto-section")?.classList.toggle("d-none",isScheduled)})}
document.querySelectorAll(".mode-toggle .btn").forEach((button)=>{button.addEventListener("click",()=>{const wrap=button.closest(".mode-toggle")||document;wrap.querySelectorAll(".btn").forEach((btn)=>btn.classList.remove("active"));button.classList.add("active")})});document.querySelectorAll(".iris-range").forEach((range,i)=>{const number=document.querySelectorAll(".iris-number")[i];range.addEventListener("input",()=>(number.value=range.value));number.addEventListener("input",()=>{const val=Math.min(100,Math.max(0,Number(number.value)||0));number.value=val;range.value=val})});document.querySelectorAll(".auto-range").forEach((range,i)=>{const number=document.querySelectorAll(".auto-number")[i];range.addEventListener("input",()=>(number.value=range.value));number.addEventListener("input",()=>{const val=Math.min(100,Math.max(0,Number(number.value)||0));number.value=val;range.value=val})});(function ensureInitialActive(){const activeBox=document.querySelector(".content-box.active");if(activeBox){const anyActiveTabContent=activeBox.querySelector(".global-tab-content.active");const anyActiveBtn=activeBox.querySelector(".global-tab-btn.active");if(!anyActiveTabContent&&!anyActiveBtn){activateFirstGlobalTabIn(activeBox)}
return}
const firstMainLink=document.querySelector(".main-link");if(firstMainLink){firstMainLink.click()}else{const firstBox=document.querySelector(".content-box");if(firstBox){firstBox.classList.add("active");activateFirstGlobalTabIn(firstBox)}}})()